export const requiredMessage = 'common.required-field';
export const maxMessage = (num: number) => `Number should be less than ${num}`;
export const minMessage = (num: number) =>
  `Number should be larger than ${num}`;
